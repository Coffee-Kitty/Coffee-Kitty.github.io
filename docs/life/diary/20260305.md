排除sphnix-doc暂时， 因为它需要外部数据源
0. 尝试在前50条数据上跑下定位


**衍生出一个问题：**
这俩test测的一个函数的不同方面，这都locate right了，这算数嘛？？
```python 
@requires_cftime
def test_groupby(da):
    result = da.groupby("time.month").sum("time")
    expected = xr.DataArray([4, 6], coords=[[1, 2]], dims=["month"])
    assert_identical(result, expected)

def test_groupby_attr_retention(self) -> None:
        da = xarray.DataArray([1, 2, 3], [("x", [1, 1, 2])])
        da.attrs = {"attr1": 5, "attr2": "history", "attr3": {"nested": "more_info"}}
        original_attrs = da.attrs

        # Test default behaviour
        result = da.groupby("x").sum(keep_attrs=True)
        assert result.attrs == original_attrs
        with xarray.set_options(keep_attrs="default"):
            result = da.groupby("x").sum(keep_attrs=True)
            assert result.attrs == original_attrs

        with xarray.set_options(keep_attrs=True):
            result1 = da.groupby("x")
            result = result1.sum()
            assert result.attrs == original_attrs

        with xarray.set_options(keep_attrs=False):
            result = da.groupby("x").sum()
            assert result.attrs == {}
```

第一个：测 groupby+sum 的数值/结构正确性（尤其是 time.month + cftime 场景）。

第二个：测 同样的 groupby+sum 在 attrs 保留策略上的行为（keep_attrs 参数和全局 option）




1. 继续昨天的10个case的分析

目前10个case，定位2个  可修复1个
    今天的主要目标是  根据之前的人工检查结果，查看定位率弱的原因
    想办法合理地提升定位率

限于test data在外部文件库的case:
test_autodoc_ignore_module_all 

top3定位成功的2个case:
test_iforest_parallel_regression
test_groupby_grouping_errors

现在挑case查看原因：

- test_identify_removable_identity_matrices

def test_identify_removable_identity_matrices():

    D = DiagonalMatrix(MatrixSymbol("D", k, k))

    cg = _array_contraction(_array_tensor_product(A, B, I), (1, 2, 4, 5))
    expected = _array_contraction(_array_tensor_product(A, B), (1, 2))
    assert identify_removable_identity_matrices(cg) == expected

    cg = _array_contraction(_array_tensor_product(A, B, C, I), (1, 3, 5, 6, 7))
    expected = _array_contraction(_array_tensor_product(A, B, C), (1, 3, 5))
    assert identify_removable_identity_matrices(cg) == expected

    # Tests with diagonal matrices:

    cg = _array_contraction(_array_tensor_product(A, B, D), (1, 2, 4, 5))
    ret = identify_removable_identity_matrices(cg)
    expected = _array_contraction(_array_tensor_product(A, B, D), (1, 4), (2, 5))
    assert ret == expected

    cg = _array_contraction(_array_tensor_product(A, B, D, M, N), (1, 2, 4, 5, 6, 8))
    ret = identify_removable_identity_matrices(cg)
    assert ret == cg

经过一番周折

认为这个fast_path优化是可以被接受的


2. 跟师兄交流了下已有的困惑

针对数据失衡问题，每条instace最多选5条test， 构成130条数据

不用给issue description，因为优化策略问题，定位后还需要策略知识注入



3. 发现个大问题1， 这里一般命中是因为， 经过压力测试裁剪后的 firegraph tree分支极少，所以top3基本都命中
复杂例子基本没有效果

问题2: 当llm去进行大规模数据构造时，这部分时间也算在了root中，是否为带来噪声。

问题3: llm挑选多个参数，然后挨个给出stress版本，最后综合一个stress test进行定位？


工程可控性问题：

可以让 LLM 先输出一个 stress 方案 + 一个可调的 scale 参数，比如：

SCALE = 2, 4, 8, 16...
然后程序自动试跑几次，找到刚好满足目标、又不超时的 SCALE。

这会把 Stage0 的成功率从“靠运气”变成“工程可控”。


4. 改下代码，输出原本的tree，以及压力测试后的tree
看下是否符合预期： 
   原来有一颗调用时间数树， 我们现在施加了压力（尽量保持同构），在这种情况下各个节点会发生变化， hot 路径会更集中， llm理论就是在这条路径上根据函数名以及自己的知识，找出来从哪里改。



5. 再看一遍10个case，重点看能否合理的丛firegraph text推出 human patch的函数
下一步我看下之前10个case的human patch函数的tottime怎么样。


