## 编写了guided locator

在astropy 12701这个case上，找到了和 human patch 同样的优化策略

```bash
[LLM OUTPUT]
{
  "candidate_edit_functions": [
    "/testbed/astropy/time/formats.py:360:_select_subfmts"
  ],
  "optimization_strategy": "Add an early-return short-circuit at the top of _select_subfmts: if pattern == '*', immediately return [x for x in cls.subfmts] (or just ['*'] when cls.subfmts is empty, per docstring semantics). This avoids the expensive fnmatch-based list comprehension — which dominates self-time — for the overwhelmingly common case (e.g., replicate, _set_scale, and _apply all pass '*' by default). The fix preserves full backward compatibility: validation behavior for non-wildcard patterns is unchanged, and the '*' path remains semantically correct per the method’s own docstring ('a special case is a format with no allowed subformats... and pattern=\"*\". This is OK'). No caching or state is needed — the optimization is purely structural and eliminates ~95% of _select_subfmts calls (17.5M → ~0.8M) without altering logic flow.",
  "evidence_chain": [
    "workload (20 calls) → Time(np.arange(...)) triggers massive array instantiation",
    "→ core.__add__ (1.75M calls) → core._set_scale (3.5M calls) → formats.__init__ (7M+ calls) → in_subfmt/out_subfmt setters (7M+ calls each)",
    "→ BUT dominant path is core._apply (1.75M calls) → core.replicate → formats._get_allowed_subfmt (3.5M calls, twice per _apply)",
    "→ formats._get_allowed_subfmt unconditionally calls _select_subfmts (even for '*' → 17.5M total calls)",
    "→ _select_subfmts (line 370) spends >20s in fnmatchcase inside a list comprehension that is fully redundant for pattern=='*'",
    "→ profiler shows _select_subfmts has highest call count (17.5M) and high self-time (16.3s), directly attributable to this unchecked hot path"
  ]
}
```


下一步简单优化下这个代码的各个方面









