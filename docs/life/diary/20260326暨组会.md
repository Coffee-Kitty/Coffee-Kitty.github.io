
## 组会总结

换数据集有一个前提，
1是 分析在sweperf上的hotspots效果不好
2是 workload确实非常非常对


### 1. 更换数据集为SWE-EFFICIENCY
   
主要区别是 从**workload**入手

**swe-perf**混合了correctness、performance test，
软件存储库通常**将正确性和性能测试分开**（ISO/IEC，2011），
correctness test基本没有一点用。
性能工程师描述工作负载的特征（由于多种原因，工作负载可能会很慢）；确定干预地点；识别并执行现有的单元测试，以确保优化不会引入新功能。


### 2. 进展
抽了一个小数据集 9个repo各5个

重跑no stress下的locate

deepseekv3.2
总共45条，成功跑了43条，2条error
完全没命中的有15条
命中有的共计28条

**正在做**:
观察调整stress生成，存在**超时,只要求改数据规模**等问题

然后**对比stress/base 和 base/patch下的topk**，
这个前面简单比过，但需要再在确认好stress的基础下，再次对比


**small问题**：
workload数据规模已经较大，为什么不缩小n，然后观察试试。。。

**small问题**:
前面由于stress生成的没法用，所以就分析了13条instance，
结果是topk重叠较高,top1就达到0.5，但是哪怕到top10，这些hotspots才开始命中human patch，

**small问题**：ground truth还是存在改了太多func
还发现有的ground truth改了太多个func
=== Ground Truth Distribution ===
Total instances: 45
  total_ground_truth == 1:  21 (46.7%)
  total_ground_truth > 1:   22 (48.9%)
  total_ground_truth > 10:  5 (11.1%)

**问题**: 在看workload时，发现遗漏了problem_statement,但是又发现这还存在空的，还需要看下。

在你当前 logs/llm_no_stress 这 45 个实例里，35 个的 problem_statement 是空字符串或只含空白。
在整个 dataset test split 里，空的 problem_statement 一共有 394 个。




## day
1. 数据集终于下载完了，共498个images
然后每个repo抽5条构建小数据集 lite,共45条



2. 重跑no stress下的locate
deepseekv3.2
总共45条，成功跑了43条，2条error
完全没命中的有15条
命中有的共计28条

还发现有的ground truth改了太多个func
=== Ground Truth Distribution ===
Total instances: 45
  total_ground_truth == 1:  21 (46.7%)
  total_ground_truth > 1:   22 (48.9%)
  total_ground_truth > 10:  5 (11.1%)


3. 观察改进stress生成
理解45个workload，思考是否只stress数据规模


似乎不能用于压力测试的workload：


数据workload分析：

**数据规模**：放大 N/size/n_variables/periods，

```python
def setup():
    global t, icrs 
    t = Time('2020-10-01T20:00') + np.random.uniform(0, 1, size=100000) * u.min
    icrs = SkyCoord.from_name('Crab')

def workload():
    icrs.transform_to(CIRS(obstime=t))#坐标系统转换

runtimes = timeit.repeat(workload, number=1, repeat=3, setup=setup)

```


**似乎放大循环次数也是对的？**    或放大 workload() 内已有循环次数；
```python
import timeit
import statistics
import astropy.units as u

#这样子施加stress
u.Unit('erg cm-2 s-1 Hz-1', format='fits')
# def workload():
#     u.Unit('km/s', format='fits')

runtimes = timeit.repeat(workload, number=1, repeat=2000)

print("Mean:", statistics.mean(runtimes[-1000:]))
print("Std Dev:", statistics.stdev(runtimes[-1000:]))
```

```python
import timeit
import statistics
from matplotlib.colors import _to_rgba_no_colorcycle
import numpy as np

# def setup():
#     global t
#     t = (0.2, 0.5, 0.8, 0.3)

def workload():
    global t
    _to_rgba_no_colorcycle(t)

runtimes = timeit.repeat(workload, number=10, repeat=2000, setup=setup)

print("Mean:", statistics.mean(runtimes))
print("Std Dev:", statistics.stdev(runtimes))

#施加数据stress
def setup():
    global t
    t = np.array([0.2, 0.5, 0.8, 0.3], dtype=np.float64)
```

对 plot/render 类，不要继续放大底层数据规模，而是放大布局/渲染次数或对象数量；
对 cache/reset 类，优先放大输入规模，不要简单包一层重复循环，否则热点会漂到 cache 行为上。
像 dask__dask-10428 这种已经很重的 case，不要追求 5x-10x，目标应该自适应降到 1.5x-3x。


**意识到problem_statement**，但是老是空的

在你当前 logs/llm_no_stress 这 45 个实例里，35 个的 problem_statement 是空字符串或只含空白。
在整个 dataset test split 里，空的 problem_statement 一共有 394 个。

**需要查论文看下。论文中不给llm probelm target。agent的目的就是通过workload去优化**


4. 分析stress/base , base/patch，hotspots topk重叠


5. 观察topk推到edit func的逻辑，似乎可以从论文附录中查看到swe-agent的基本逻辑，从而优化







改数据规模
> 有setup，压力setup
> 可能只有workload，也能压力
> 还会存在setup、workload、然后供setup调用的函数







> 数据排除
> scikit-learn-10610.
> 这里workload居然要从server = 'https://raw.githubusercontent.com/fgnt/mnist/master'下载minist数据集，然后再
> X, _ = load_mnist()
> data = X[:10000]
> 虽然能测试，但是网络不稳定，没法搞。
>
> 



今天看了45个workload，发现是都能够通过数据规模做stress的
然后写好stress的代码，要求stress_workload变慢，并且不能超过容器的timeout

然后接下来主要是运行这个版本，接着查验代码

