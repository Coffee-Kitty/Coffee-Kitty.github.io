1. 看了 wedge


他profiler了 slow 和 fast input，
然后用llm 根据这个hit count信息，生成约束，
接着用这个约束去做fuzz。 即生成mutator然后用AFL++去fuzz。

最后，做了一个数据集。但是方法本身没开源。


相比coffe， 进一步多了个fuzz的理念。



2. 再次梳理sweperf数据集，
这数据集真的合适吗？

设计实验探究
    apply前后hotspots（分为直接的， 过滤出只包含内部库的）cum， tot，calls，对test func的占比， rank变化如何？



    直接hotspots func中占内部库多少？命中human patch多少？

    





3. hotspots代码查验

验证前后hotspot的profile是否会发生变化

目前看是变化了，但是很反直觉

下一步确认下代码写没写错



4.

好消息：

第一步：固定输入跑一次 profile（baseline tree）。
- 第二步：fuzz 输入跑 N 次，汇总 top-k 热点（fuzz tree）。
- 第三步：做 delta 排名，优先看 cum_delta + self_delta 同时上升的函数（比只看 cum_time 更稳）。
- 第四步：把 top_deltas 直接喂给 LLM，要求只在这些函数里给 patch 候选，避免全仓乱改。



5.
坏消息

之前的test筛选有问题，用的是一个instance对应的多个test的执行时间，当作一个test的时间

现在重新执行test以获取结果执行时间，然后再过滤
