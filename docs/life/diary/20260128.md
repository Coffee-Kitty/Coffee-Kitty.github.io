> 现在的定位， 送入llm的信息， 利用反馈信息  三个步骤都没做好还


1.定位率试验
提前human patch中 有过修改的function，作为参考基准

定位实在是不准。







### 直接将整个背景文件送入llm，是不是太粗糙了
这里我们需要讨论下，我想的基本思路是， 将待优化函数的上级和下级直接调用函数同时给llm，让llm修，此外stage4的功能性测试以及性能测试结果也应该作为反馈信息提供给llm， llm在指定迭代次数这里迭代修复





2.idea
目前的定位绝对是太拉了

2.1方法思路是 llm从最初的test出发，依据cprofiler生成的调用链，先判断下那个函数是被测函数，然后尝试生成压力测试。
然后结合调用链从被测函数开始逐步插桩line_profiler，每次先思考这里是否能有较大性能提升，如果能的话，输出优化code，否则思考哪些函数调用很可疑，收集进topk可疑点中，然后向下重复该动作。直到遇到调用为外部调用。此时，若还是没有可优化点，那么该分支结束，进入上述dfs的另一分支，接着判断。最终若整个过程均无较大性能提升点。则什么也不生成。


2.2 上述如果是多层嵌套调用的话，容易进行太多冗余操作
能否直接将cProfiler tree给llm， llm思考下哪些函数最可疑， 然后一起插桩这些

> 下面这个倒是一定程度可行的